# Food Reel App

This project was generated manually because the Flutter SDK was not found in the environment.

## Getting Started

1.  Make sure you have Flutter installed.
2.  Open a terminal in this directory.
3.  Run `flutter create .` to generate platform-specific files (android, ios, web, etc.).
4.  Run `flutter pub get` to install dependencies.
5.  Run `flutter run` to start the app.
